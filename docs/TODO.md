# TODO

## 2026-03-15

- Refine market-regime logic so weak breadth throttles sizing while panic tape remains the only hard veto.
- Enforce `min_cash_pct` in code, not only in prompt text.
- Size new positions from available cash instead of starting capital.
