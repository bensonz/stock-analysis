# Worklog 2026-07-27 → 07-31 (action → result, verified against commits)

## 07-27
- **Committed stragglers** → `4ad773b` (7/24 run manifest), `8b766bd` (3 prediction bets: 688099 j1, 605098 momentum_gate_pass p=0.6792 + j1), `bfcc583` (reports/ fully gitignored, flat v1-notool copies untracked).
- **Diagnosed 300037 report degradation** ("显著不为零") → the writer HAD written the exact number (39.0%, n=11442, CI 38.1–39.9); the verify pipeline destroyed it. Root causes: (1) compound number tokens ("11442，95%", "38.1–39.9") could never match the corpus mechanically; (2) a 23-claim judge batch truncated at max_tokens=4096, and parse failure was treated as rejection → numbers scrubbed.
- **Fix 1** → `2400b7b`: part-wise token matching (all constituents must be in corpus — stricter AND correct), dict-key digits added to corpus (wilson95→95), RPS60≥90-style comparisons allowlisted.
- **Fix 2** → `f82555e`: new `judge_error` status (outage ≠ rejection), judge batches chunked at 20, max_tokens 8192, re-judge next round instead of cache-pinning; persistent outage KEEPS numbers with footer disclosure `⚠️N处未复核`. Audit scan: 300037 + 688099 degraded (rerun-worthy), 002245/605098/688378 clean.

## 07-30
- **Investigated the losing streak** → 7/27 batch (5 entries on an 18:1 euphoric day) all stopped 7/28; 7/29 batch bought the bounce, 600176 (-5.91%) & 603259 (-3.56%) cut on 7/30's panic (breadth 0.55:1, 创业板 -5.89%). ~-0.9pp portfolio over both batches.
- **Breadth study** (86 days, 48,794 obs) → entry-day market breadth does NOT predict 3-day stop risk; baseline ~40–44% stop rate in the very zone the gate permits buying; euphoric ≥6:1 days were actually the BEST (26.7%) — my own throttle hypothesis refuted. Bounce days bimodal (85%/60% tail days).
- **Pool-pain study** → >60% of gate pool down ≥3%/2d ⇒ 55.2% fwd stop rate, fwd5 -6.15%; sweet spot 15–30% pain ⇒ 28.1%, +1.68%.
- **Coverage-floor** → first claimed an active bug, then CORRECTED: not active (floor 4970 vs ≥5182-row days). Latent drift real (all-time distinct-code denominator only grows) → fixed to p80-of-recent-60-days, verified behavior-neutral on all 393 dates → `deec1b6`.
- **Backtest harness commissioned** → plan `a11f226`; Stage 1a engine `de24b67` (T+1, limit-up entry skip / limit-down exit deferral, 0.30% costs, adjusted prices, 12 synthetic tests); Stage 1b `80df134` (mechanical-ANALYST arm + replay of the 41 real trades: model residual **0.30pp** ≈ exactly the cost load — model sound; entry fill-timing noise 2.53pp; 21/41 trades unreplayable due to partial-day gaps); Stage 1c `3666196`:
  - **Verdict: mechanical ANALYST.md = -28.98% net (Jan–Jul), EW market -14.89%, live portfolio -5.9%.** The LLM/pipeline's selectivity+cash beat its own written ruleset by 23pp. 148 trades, 8.1% win rate, -5% stops realized -8.44% avg.
- **Stage 2 experiments** → `4de6355`: pool_pain -20.6 / wide_stop -12.1 / pullback -8.9 / disciplined -24.7 / **combo -2.48 (maxDD 10.6, 51 trades)**. Nothing beat cash; combo ≈ formalized "mostly don't trade". In-sample warning attached — NOT shipped.

## 07-31
- **Rank-IC audit of the picker** → RPS60 within the gate pool: mean IC **-0.063** (5d), positive only 36% of days, top decile trails bottom by 1.1pp. Harness confirm: rank flipped alone takes baseline -28.98% → **-3.47%**; combo+flip -1.87%, maxDD 5.1%. Picker was INVERTED, not weak. Inversion NOT shipped (regime-dependent sign) → `7884150`. Horizon sweep: inversion peaks at 10d, decays ~0 by 30d.
- **Kronos spike** → isolated venv, Kronos-small, 27 days × ~550 stocks zero-shot (53min MPS; found upstream bug: `from_pretrained` leaves training mode on → `model.eval()` required). Result: IC +0.056 vs RPS60 -0.071, **but** 0.70 ranking overlap with inverted-RPS60, IC-series corr -0.89, orthogonalized residual **+0.008 ≈ nothing**; plain inverted RPS60 scores +0.071 free. **Audition failed** → `897cb0f`; artifacts deleted on user order → `aa19e13` (audit record + scores kept in docs/backtest/).
- **RPS band restored** → user identified "pick from the top" as wrong; traced to `f65cd97` (7/22, LLM-argued cap removal). Receipts: post-removal entries 冰轮环境 99.1 / 中国巨石 98.7 / 昊志机电 98.5 / 广合科技 98.0 took the deepest 7/27–7/29 losses. Reverted to sweet-spot design (75–95, 80–92 core, >95 skip) + "empirically load-bearing" marker + LEARNINGS post-mortem → `c5c91c0`.
- **Event system** → verified real 2026 calendar (FOMC WAS 7/28–29 — system bought on decision day; Hormuz re-escalation 7/7–10 = pool-massacre week; hot CPI 7/14 = worst entry day). Built `tracking/events.json` + `scripts/event_calendar.py` (US→A-share impact-date shift, risk levels, measured FOMC base rate: **9/12 next sessions red, mean EW -0.45%**), phase-1 artifact, prompt section, ANALYST.md Rule 2c → `871a3bd`. Expanded: NVDA 8/26, Jackson Hole 8/28, 五中全会 (Oct), CEWC (Dec), 两会 (3/5), formulaic PMI/Caixin/LPR/NFP/earnings-deadlines/Golden-Week, `direction` field (🔴/🔶/🟢), report.md 未来事件窗口 section; China CPI/PPI excluded per user → `df3de13`. Anti-freeze semantics (chronic risk ≠ trading halt) → `8b1bec9`.
- **Cost-of-caution** → `scripts/cost_of_caution.py` (replays every skip under Rule-5 exits + costs — "went up later" doesn't count if it stopped out first), WEEKLY_AUDIT.md Step 2C replaced → `5878fcc`. July: **34 disasters avoided vs 1 win missed, ≈ +11.8pp saved**.
- **Retro cost-of-caution Mar–Jul** (no PIT needed — skips were recorded at decision time) → `e3a9754`: Mar ≈flat; **Apr/May/Jun each ≈ -16pp/month of regret** (18–26 missed wins/month, incl. 海星股份 skipped 4/1 for "RPS 97 too extended" → +173%/10 sessions); Jul +11.8pp. Third independent measurement of the same Apr–Jun (momentum) vs Jul (reversal) regime boundary, after rank-IC and the experiment matrix. Numbers are regret upper bounds (position cap makes taking every skip impossible) — caveat printed by the tool.

## Standing state
- Suite: 391 passed, 7 pre-existing failures (hypothesis_manager ×6, entry_regime sizing), 10 skipped. All work pushed through `e3a9754`.
- Portfolio: 1 position (000703), ~97% cash, -5.9% total.

## Agreed next step (pending user go)
**Read-only regime detector v1** — a daily computed line in report.md (wired to nothing):
rolling ~20d rank-IC of RPS60 + trailing pool stop-rate + trailing cost-of-caution sign
→ label 趋势市/震荡回撤市. Validation: retro-compute across Mar–Jul where the answer key
is known (must read "trend" in Apr, "reversal" in mid-Jul, else it's junk). Graduation
path only after it passes: read-only → Tier-2 prompt advice → (if evidence demands)
Tier-1 mechanical dials (RPS extended-zone strictness, sizing, pullback-only entries).
Known limitation, stated upfront: trailing inputs mean it flips AFTER turns — it reduces
mid-regime bleed; the event calendar remains the only early instrument.

## Smaller open items
- Rerun degraded deep reports (300037, 688099) under the fixed verify pipeline — not yet confirmed done.
- events.json curation: pin October CPI + 五中全会/CEWC dates when announced; delete the 7月政治局 supportive entry ~mid-August.
- First regular weekly audit (Sunday) now includes cost-of-caution.
- Older backlog: pricedb gap repair via sina sweep (measured cost: 21/41 trades unreplayable), prediction_log resolve step in the afternoon run, deep-report verdict injection into daily prompt (designed, not commissioned).
